name: 同步影视源配置

on:
  schedule:
    - cron: '0 */6 * * *'  # 每 6 小时执行一次
  workflow_dispatch:  # 支持手动触发

permissions:
  contents: write  # 明确授予写入权限

jobs:
  sync:
    runs-on: ubuntu-latest
    steps:
      - name: 检出仓库
        uses: actions/checkout@v4
        with:
          ref: main  # 直接操作 main 主分支

      - name: 拉取配置
        run: |
          curl -o config.txt "https://odd-hall-95d8.1430066373.workers.dev/?config=1&encode=base58"
          echo "✅ 配置文件下载完成"
          echo "📦 文件大小: $(wc -c < config.txt) bytes"

      - name: 提交更新
        run: |
          git config --global user.name "github-actions[bot]"
          git config --global user.email "github-actions[bot]@users.noreply.github.com"

          # 检查是否有变更
          if ! git diff --quiet config.txt 2>/dev/null || [ ! -f config.txt ]; then
            git add config.txt
            git commit -m "🔄 自动同步配置 - $(date -u +'%Y-%m-%d %H:%M:%S UTC')" || echo "没有变更需要提交"
            git push origin main
            echo "✅ 配置已同步到 main 分支"
          else
            echo "ℹ️ 配置文件没有变化，跳过提交"
          fi
